#include "lib.h"
/**
 *
 */
char **_toktok(char *buf)
{
	char **arr, *tok;
	int j = 0;

	arr = malloc(sizeof(char *) * 32);
	tok = strtok(buf, " \n\r\t");

	for (j = 0; tok != NULL; j++)
	{
		arr[j] = tok;
		tok = strtok(NULL, " \n\r\t");
	}
	arr[j] = NULL;
	return (arr);
}
